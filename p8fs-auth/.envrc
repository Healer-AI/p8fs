# P8FS Auth Development Environment
layout python-venv

# Add source directories to PYTHONPATH for real-time edits
export P8FS_MODULES_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${P8FS_MODULES_ROOT}/p8fs-cluster/src:${P8FS_MODULES_ROOT}/p8fs-core/src:${PYTHONPATH}"

echo "✓ P8FS Auth environment loaded"